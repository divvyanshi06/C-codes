#include<stdio.h>
int main(){
	char word[50];
	int i,length=0;
	printf("Enter your word: ");
	scanf("%s",word);
	for(i=0;word[i]!='\0';i++){
		length++;
	}
	printf("length is:%d",length);

	return 0;
}
